export TOKEN_SYMBOL="USDC"
export TOKEN_NAME="Test USDC"
export MAILBOX_ADDRESS=""
export INTERCHAIN_GAS_PAYMASTER_ADDRESS=""
export LAYER_ZERO_ENDPOINT=""
export TREASURY_ADDRESS=""

# Start hardhat node with specific port.
# Set ETH_NODE_URI_ to the above localhost:PORT combination.
# Run the command below.

# Copy this script but for another network with a different port. This way we can have two networks standing up locally.

npx hardhat deploy --network test